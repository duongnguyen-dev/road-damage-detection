[project]
name = "road-damage-detection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dataset-tools>=0.1.7",
    "loguru>=0.7.3",
    "torch>=2.7.1",
]
